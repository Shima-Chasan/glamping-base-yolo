---
layout: base.njk
title: glamping base YOLO - 星空とジャングルで、ととのう休暇を。
---

<!-- ヒーローセクション -->
<section class="relative h-screen">
    <!-- スライドショー -->
    <div class="slideshow-container">
        <div class="slide fade absolute inset-0 bg-cover bg-center" style="background-image: url('/images/hero1.jpg');"></div>
        <div class="slide fade absolute inset-0 bg-cover bg-center" style="background-image: url('/images/hero2.jpg');"></div>
        <div class="slide fade absolute inset-0 bg-cover bg-center" style="background-image: url('/images/hero3.jpg');"></div>
        <div class="slide fade absolute inset-0 bg-cover bg-center" style="background-image: url('/images/hero4.jpg');"></div>
    </div>
    
    <!-- オーバーレイとテキスト -->
    <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center">
        <div class="text-center text-white px-4">
            <h1 class="text-5xl md:text-6xl font-bold mb-4 font-serif">glamping base YOLO</h1>
            <p class="text-xl md:text-2xl mb-8">星空とジャングルで、ととのう休暇を。</p>
            <a href="#booking" class="bg-turquoise text-white px-8 py-3 rounded-full text-lg font-medium hover:bg-opacity-90 transition duration-300">予約する</a>
        </div>
    </div>
</section>

<!-- 特徴セクション -->
<section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-12">YOLOの魅力</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <img src="/images/feature1.jpg" alt="贅沢なテント" class="w-full h-64 object-cover">
                <div class="p-6">
                    <h3 class="text-xl font-bold mb-3">贅沢なテント</h3>
                    <p class="text-gray-600">エアコン完備の快適なテントで、自然を感じながらも快適に過ごせます。</p>
                </div>
            </div>
            
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <img src="/images/feature2.jpg" alt="絶景プール" class="w-full h-64 object-cover">
                <div class="p-6">
                    <h3 class="text-xl font-bold mb-3">絶景プール</h3>
                    <p class="text-gray-600">山々を見渡せるインフィニティプールで、贅沢なひとときを。</p>
                </div>
            </div>
            
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <img src="/images/feature3.jpg" alt="森のサウナ" class="w-full h-64 object-cover">
                <div class="p-6">
                    <h3 class="text-xl font-bold mb-3">森のサウナ</h3>
                    <p class="text-gray-600">自然に囲まれたサウナで、心も体もリフレッシュ。</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- お知らせセクション -->
<section class="py-16">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-12">お知らせ</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {% for article in news | slice(0, 3) %}
            <div class="bg-white rounded-lg shadow-md overflow-hidden news-item">
                <div class="p-6">
                    <h3 class="text-xl font-bold mb-2 line-clamp-1">{{ article.title }}</h3>
                    <p class="text-gray-600 mb-4">{{ article.date }}</p>
                    <div class="text-gray-700 mb-4 line-clamp-3">
                        {{ article.content | striptags | truncate(100) }}
                    </div>
                    <a href="/news/{{ article.id }}/" class="text-turquoise hover:underline">詳しく見る</a>
                </div>
            </div>
            {% endfor %}
            
            {% if news.length === 0 %}
            <div class="col-span-full text-center py-8">
                <p>現在お知らせはありません。</p>
            </div>
            {% endif %}
        </div>
        
        <div class="text-center mt-8">
            <a href="/news/" class="inline-block border-2 border-turquoise text-turquoise px-6 py-2 rounded-full hover:bg-turquoise hover:text-white transition duration-300">もっと見る</a>
        </div>
    </div>
</section>

<!-- Eleventyでページが生成されるため、動的なロードとモーダルは不要 -->

{% block scripts %}
<style>
    /* スライドショー用のスタイル */
    .slideshow-container {
        position: absolute;
        width: 100%;
        height: 100%;
    }

    .slide {
        opacity: 0;
        transition: opacity 1.5s ease-in-out;
    }

    .fade {
        animation-name: fade;
        animation-duration: 16s;
        animation-iteration-count: infinite;
    }

    .slide:nth-child(1) {
        animation-delay: 0s;
    }

    .slide:nth-child(2) {
        animation-delay: 4s;
    }

    .slide:nth-child(3) {
        animation-delay: 8s;
    }
    
    .slide:nth-child(4) {
        animation-delay: 12s;
    }

    @keyframes fade {
        0%, 22% { opacity: 1; }
        25%, 97% { opacity: 0; }
        100% { opacity: 1; }
    }
    
    /* line-clampクラスのスタイル定義 */
    .line-clamp-1 {
        display: -webkit-box;
        -webkit-line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
</style>
{% endblock %}
